const {
    makeRequest,
    verifyParameters
} = require('../../lib/api');

const publishConfigurations = ({
    API_KEY
}) => {
    let self = this;

    return self;
};

module.exports = publishConfigurations;